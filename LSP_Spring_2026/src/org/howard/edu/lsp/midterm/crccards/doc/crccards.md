TaskManager collaborates with Task because its responsibilities are to store tasks, add new tasks, find a task by ID, and return tasks by status, each requiring it to work directly with Task objects. However, Task doesn't collaborate with TaskManager because its own responsibilities are only to store task information, update task status, and provide task details. Thus, it doesn't need to manage or even know about the larger collection of tasks.
